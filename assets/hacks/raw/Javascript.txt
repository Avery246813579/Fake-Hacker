package me.averydurrant.fileconverter;··public class MainFrame {·øpublic static void main(String[] args) {·øønew MainFrame();·ø}··ø/*·ø * Let's create some sets for hacking·ø */·øprivate final String ALPHABET_SET = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz";·øprivate final String DIGIT_SET = "0123456789";·øprivate final String CHAR_SET = ALPHABET_SET + DIGIT_SET;··øpublic MainFrame() {·øøString ip = findInternetProtocolAddress();·øøSystem.out.println(ip);·ø}··øpublic String findInternetProtocolAddress() {·øøint[] sections = {2, 3, 2, 3};·øø·øøString ip = "";·øøfor(int section : sections){·øøøip += "." + findProtocolSection(section);·øø}·øø·øøreturn ip.substring(1);·ø}··øpublic String findProtocolSection(int max){·øøreturn findProtocolSection(0, max);·ø}·ø·øpublic String findProtocolSection(int current, int max) {·øøif (max - 1 < current) {·øøøreturn "";·øø}··øøreturn reherseCharacter() + findProtocolSection(current + 1, max);·ø}··øpublic char reherseCharacter() {·øøreturn DIGIT_SET.charAt((int) Math.floor(Math.random() * DIGIT_SET.length()));·ø}··ø/*·ø * - FIND IP - GET USERNAME - FIND PASSWORD - Get it one way then reverse it·ø * - Log in·ø */·}